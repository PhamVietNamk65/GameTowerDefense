package entity.monster;

import static utils.Constants.Monsters.MAGE;
import static utils.Constants.Monsters.RAT;

public class Mage extends Monster {
    public Mage(float x, float y, int ID) {
        super(x, y, ID, MAGE);
    }
    
}
